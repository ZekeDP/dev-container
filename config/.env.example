# Copy to ../.env
# cp config/.env.example .env

# Repository Configuration
BACKEND_REPO=git@github.com:ZekeDP/car-rental-app-be.git
CLONE_METHOD=ssh

# Application Ports
BACKEND_PORT=8080
FRONTEND_PORT=5173

# Spring Boot
SPRING_PROFILES_ACTIVE=dev

# Database (Optional)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=car_rental_dev
DB_USER=dev_user
DB_PASSWORD=dev_password